interface:
  display_name: "Evmsec"
  short_description: "Repo-specific patterns and workflows for evmsec"
  default_prompt: "Use the evmsec repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true